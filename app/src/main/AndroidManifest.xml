<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sensorrecorder">

    <!-- 通知权限（光线超限告警 + CSV导出成功通知） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 后台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <!-- CSV文件导出存储权限 -->
    <!-- Android 13及以下读写外部存储 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <!-- Android 11~12 完整文件访问（可选，解决部分机型下载目录写入失败） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" android:maxSdkVersion="34"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sensorrecorder">

        <!-- 主启动页面 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 设置页面 -->
        <activity
            android:name=".SettingActivity"
            android:exported="false"/>

        <!-- 历史列表页面（CSV导出功能所在页面） -->
        <activity
            android:name=".DataListActivity"
            android:exported="false"/>

        <!-- 后台采集服务 -->
        <service
            android:name=".SensorCollectService"
            android:exported="false"/>

    </application>
</manifest>